services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_DB: proofdesk
      POSTGRES_USER: proofdesk
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    # Only exposed internally; not bound to host port

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    restart: unless-stopped
    depends_on:
      - postgres
    environment:
      POSTGRES_DSN: postgresql+asyncpg://proofdesk:${POSTGRES_PASSWORD}@postgres:5432/proofdesk
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL}
      OPENAI_MODEL: ${OPENAI_MODEL}
      DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-v4}
      RERANKER_MODEL: ${RERANKER_MODEL:-gte-rerank}
      EMBED_DIM: ${EMBED_DIM:-1024}
      ADMIN_USERNAME: ${ADMIN_USERNAME}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    volumes:
      - questionnaire_files:/app/questionnaire_files

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "127.0.0.1:8080:80"

volumes:
  pgdata:
  questionnaire_files:
