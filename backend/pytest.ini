[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    smoke: deterministic end-to-end regression coverage for the customer-critical path
    live_eval: tests that require real LLM or embedding providers
